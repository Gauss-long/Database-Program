# 高校竞赛项目管理系统项目结构说明

本项目是一个面向高校竞赛、科研训练和创新创业项目的管理系统。系统围绕学生、教师、团队、竞赛、项目、报名、成果和经费等数据展开，主要完成信息维护、关联查询和统计展示。

项目开发重点放在数据库关系的完整表达和业务流程的清晰展示上。系统页面保持简洁，功能覆盖课程设计要求，可视化部分用于展示数据统计结果，方便在答辩中说明数据库设计和查询逻辑。

## 1. 系统定位

系统名称：高校竞赛项目管理系统

系统主要完成以下工作：

- 维护学生、教师、团队、竞赛、项目、报名、成果和经费信息。
- 展示用户与学生、教师之间的继承关系。
- 展示学生与团队、团队与竞赛之间的多对多关系。
- 展示项目与团队、教师、成果、经费之间的关联关系。
- 提供典型 SQL 查询和统计功能。
- 通过首页图表展示项目状态、竞赛报名、经费分布等统计结果。

## 2. 技术结构

项目采用前后端分离结构。

前端负责页面展示、表单录入、列表筛选和图表展示。  
后端负责接口处理、业务校验和数据库访问。  
数据库负责保存核心业务数据，并通过主键、外键和关系表维护数据关系。

技术组成：

- 前端：Vue 3、Vite、Element Plus、ECharts
- 后端：Spring Boot 或 FastAPI
- 数据库：MySQL 或 SQL Server
- 接口：REST API

如果后续使用 Java 开发，后端目录按 Spring Boot 组织；如果使用 Python 开发，后端目录按 FastAPI 组织。整体模块划分保持一致。

## 3. 目录结构

```text
Database-Program/
|-- README.md
|-- PROJECT_STRUCTURE.md
|-- docs/
|   |-- database-design.md
|   |-- api-design.md
|   |-- demo-plan.md
|-- database/
|   |-- schema.sql
|   |-- seed.sql
|   |-- queries.sql
|-- backend/
|   |-- src/
|   |   |-- controllers/
|   |   |-- services/
|   |   |-- repositories/
|   |   |-- models/
|   |   |-- main.*
|   |-- config/
|   |-- README.md
|-- frontend/
|   |-- src/
|   |   |-- api/
|   |   |-- views/
|   |   |-- components/
|   |   |-- router/
|   |   |-- stores/
|   |   |-- main.*
|   |-- README.md
|-- screenshots/
|   |-- demo-images/
```

## 4. 数据库部分

数据库部分放在 `database/` 目录下。

`schema.sql` 用于创建数据表、主键、外键和必要约束。  
`seed.sql` 用于插入演示数据。  
`queries.sql` 用于保存课程设计中需要展示的典型查询语句。

核心数据表如下：

- `Users`：用户基础信息表。
- `Student`：学生信息表，使用 `UserID` 关联 `Users`。
- `Teacher`：教师信息表，使用 `UserID` 关联 `Users`。
- `Team`：团队信息表，记录团队名称、成立时间和负责人。
- `Participate`：团队成员关系表，表示学生加入团队。
- `Competition`：竞赛信息表。
- `Project`：项目信息表，关联团队和指导教师。
- `Competition_Project`：竞赛项目扩展表。
- `Research_Project`：科研项目扩展表。
- `Registration`：竞赛报名表，表示团队报名参加竞赛。
- `Achievement`：项目成果表。
- `Funding`：项目经费表。

`Registration` 表保留报名时间和报名状态字段，用来对应系统需求中的报名管理功能。主键字段在实际实现中使用自增编号，方便前后端进行新增、编辑和删除操作。

## 5. 后端部分

后端部分放在 `backend/` 目录下。

后端按业务分层组织：

- `controllers/`：接口层，接收前端请求并返回 JSON 数据。
- `services/`：业务层，处理数据校验和业务规则。
- `repositories/`：数据访问层，执行数据库查询。
- `models/`：数据模型层，对应数据库表结构。
- `config/`：数据库连接和系统配置。

后端主要提供以下接口模块：

- 学生管理接口
- 教师管理接口
- 团队管理接口
- 团队成员接口
- 竞赛管理接口
- 竞赛报名接口
- 项目管理接口
- 项目成果接口
- 项目经费接口
- 首页统计接口

业务校验主要放在 Service 层。例如创建项目时，需要确认团队和指导教师存在；添加团队成员时，需要确认学生存在；登记成果或经费时，需要确认项目存在。

## 6. 前端部分

前端部分放在 `frontend/` 目录下。

前端页面采用管理台形式，左侧为功能导航，右侧为主要内容区。首页展示统计数据，其余页面负责具体数据维护。

主要页面如下：

- 首页看板
- 学生管理
- 教师管理
- 团队管理
- 竞赛管理
- 项目管理
- 成果管理
- 经费管理

首页看板展示以下内容：

- 项目总数
- 竞赛总数
- 团队总数
- 成果总数
- 项目状态分布图
- 各项目经费统计图
- 竞赛报名数量排行
- 教师指导项目数量统计

列表页面统一包含查询、表格、新增、编辑、删除和详情查看功能。详情页面重点展示关联数据，例如项目详情中展示所属团队、指导教师、项目成果和经费记录。

## 7. 查询与统计

项目保留一组典型 SQL 查询，用于体现数据库课程设计要求。

主要查询包括：

- 查询所有进行中的项目。
- 查询每个项目的所属团队和指导教师。
- 查询某个学生参与的团队和项目。
- 查询某个教师指导的全部项目。
- 查询某个竞赛的报名团队。
- 查询某个项目的成果记录。
- 统计每个项目的经费总额。
- 统计不同状态的项目数量。
- 统计不同级别竞赛的数量。

这些查询覆盖单表查询、多表连接、嵌套查询、`EXISTS` 查询和聚合查询。前端首页中的图表数据也由这些统计查询整理得到。

## 8. 开发流程

项目按以下顺序推进：

1. 整理数据库建表脚本，完成 `schema.sql`。
2. 准备演示数据，完成 `seed.sql`。
3. 完成后端基础查询接口。
4. 完成前端列表页面。
5. 补充新增、编辑、删除和详情功能。
6. 完成统计接口。
7. 完成首页可视化图表。
8. 进行整体联调，整理答辩演示流程。

## 9. 答辩展示流程

答辩时按系统的数据逻辑展开说明。

1. 说明业务背景：高校竞赛和项目数量增加，需要统一管理。
2. 说明核心实体：用户、学生、教师、团队、竞赛、项目、成果和经费。
3. 说明表间关系：继承关系、多对多关系、一对多关系。
4. 展示系统首页：通过图表说明系统中的统计数据。
5. 展示管理页面：演示项目、竞赛、团队和人员数据维护。
6. 展示典型查询：演示教师指导项目、团队报名竞赛、项目经费统计等查询。
7. 总结系统功能：系统实现了数据维护、关系管理、查询统计和可视化展示。

## 10. 当前版本范围

当前版本以课程设计展示为目标，功能范围控制在核心业务内。

本版本包含：

- 数据库建表脚本。
- 演示数据。
- 核心数据管理页面。
- 关联详情页面。
- 统计查询接口。
- 首页可视化图表。
- 答辩所需的典型 SQL 查询。

暂不包含复杂权限管理、消息通知、文件上传审批流和多角色工作流。这些内容不影响数据库课程设计的核心展示，后续需要时可以继续扩展。
