# 高校竞赛项目管理系统项目结构说明

本项目用于完成数据库工程作业。系统采用 B/S 结构，围绕高校竞赛、项目、团队、学生、教师、成果和经费等信息进行管理。项目重点不放在复杂业务功能上，而是完整体现数据库工程作业要求中的连接数据库、关系表设计、事务、触发器、存储过程和视图查询。

## 1. 作业要求对应关系

作业要求中需要在报告和程序中展示四类数据库操作。本项目按以下方式对应：

| 作业要求 | 项目中的实现 |
| --- | --- |
| 含有事务应用的删除操作 | 删除项目时，同时删除项目成果、经费记录、项目扩展表记录，再删除项目主表记录 |
| 触发器控制下的添加操作 | 团队报名竞赛时，由触发器检查竞赛时间、团队成员等条件 |
| 存储过程控制下的更新操作 | 调用存储过程更新项目状态或报名状态，并同步处理关联表数据 |
| 含有视图的查询操作 | 建立项目综合视图，查询项目、团队、教师、成果数量和经费总额 |

这样安排后，系统页面不需要做得特别复杂，但每个评分点都有明确的数据库逻辑和可演示入口。

## 2. 技术结构

项目采用前后端分离结构。

- 前端：Vue 3、Vite、Element Plus、ECharts
- 后端：FastAPI
- 数据库：MySQL
- 接口：REST API

数据库使用 MySQL，满足作业中“后台数据库使用大型数据库管理系统”的要求。高级语言使用 Python 3.9.19，后端框架使用 FastAPI。

## 3. 调整后的目录结构

```text
Database-Program/
|-- README.md
|-- PROJECT_STRUCTURE.md
|-- docs/
|   |-- report-outline.md
|   |-- database-design.md
|   |-- connection-string.md
|   |-- demo-steps.md
|-- database/
|   |-- init.sql
|   |-- required-demo.sql
|-- backend/
|   |-- src/
|   |   |-- controllers/
|   |   |-- services/
|   |   |-- repositories/
|   |   |-- models/
|   |   |-- main.*
|   |-- config/
|   |-- README.md
|-- frontend/
|   |-- src/
|   |   |-- api/
|   |   |-- views/
|   |   |-- components/
|   |   |-- router/
|   |   |-- main.*
|   |-- README.md
|-- screenshots/
|   |-- database-diagram/
|   |-- transaction-delete/
|   |-- trigger-insert/
|   |-- procedure-update/
|   |-- view-query/
```

目录调整后，`database/` 只保留两个 SQL 文件，减少文件数量。`docs/` 负责保存报告材料，`screenshots/` 负责保存报告中需要粘贴的运行截图。

## 4. 数据库脚本说明

### 4.1 初始化脚本

`database/init.sql` 保存数据库初始化内容，包括：

- 建表语句
- 主键和外键约束
- 触发器
- 存储过程
- 视图
- 演示数据

所有数据库对象放在一个脚本里，方便一次执行，也方便报告截图。表的创建顺序按照外键依赖安排，便于报告中按顺序说明。

核心表包括：

- `Users`
- `Student`
- `Teacher`
- `Team`
- `Participate`
- `Competition`
- `Project`
- `Competition_Project`
- `Research_Project`
- `Registration`
- `Achievement`
- `Funding`

报告中说明数据表时，参照字段统一写成 `表名.字段名` 的形式，例如：

- `Student.UserID` 参照 `Users.UserID`
- `Teacher.UserID` 参照 `Users.UserID`
- `Project.TeamID` 参照 `Team.TeamID`
- `Project.TeacherID` 参照 `Teacher.UserID`
- `Achievement.ProjectID` 参照 `Project.ProjectID`
- `Funding.ProjectID` 参照 `Project.ProjectID`

### 4.2 视图

视图名称可以设置为 `v_ProjectSummary`。该视图连接项目、团队、教师、用户、成果和经费，展示项目名称、团队名称、指导教师、项目状态、成果数量和经费总额。

该视图用于完成作业中的“含有视图的查询操作”。

### 4.3 触发器

触发器可以设置在 `Registration` 表上，用于控制团队报名竞赛的添加操作。触发器检查内容包括：

- 报名团队必须存在成员。
- 竞赛结束后不能继续报名。
- 报名状态不能为空。

如果输入数据不满足触发器要求，插入操作失败，系统报错。该设计正好对应作业要求中的“违背触发器要求，不能够执行插入操作，系统报错”。

### 4.4 存储过程

存储过程可以命名为 `sp_UpdateProjectStatus`，用于根据项目成果和经费情况更新项目状态。例如：

- 项目存在成果记录，并且经费总额大于 0 时，状态更新为“已完成”。
- 项目未产生成果时，状态保持“进行中”。

该操作涉及 `Project`、`Achievement`、`Funding` 等表，满足作业中“必须包含两张或两张以上关系表”的要求。

### 4.5 作业演示脚本

`database/required-demo.sql` 保存报告中需要单独演示的 SQL，包括：

- 单表查询
- 多表连接查询
- 嵌套查询
- `EXISTS` 查询
- 聚合查询
- 事务删除演示 SQL
- 触发器添加演示 SQL
- 存储过程更新演示 SQL
- 视图查询演示 SQL

实际开发时，先执行 `init.sql` 建库和插入数据，再执行 `required-demo.sql` 截取报告需要的运行结果。

## 5. 后端结构

后端只实现报告和演示需要的核心功能。

主要接口模块：

- 学生管理
- 教师管理
- 团队管理
- 竞赛管理
- 项目管理
- 成果管理
- 经费管理
- 报名管理
- 统计查询

报告重点操作对应后端接口：

| 操作 | 后端接口 |
| --- | --- |
| 事务删除 | `DELETE /api/projects/{id}` |
| 触发器添加 | `POST /api/registrations` |
| 存储过程更新 | `PUT /api/projects/{id}/status` |
| 视图查询 | `GET /api/statistics/project-summary` |

后端代码中需要保留关键 SQL 或调用逻辑，方便截图放入报告。

## 6. 前端结构

前端页面以演示为主，页面数量控制在必要范围内。

主要页面：

- 首页看板
- 项目管理
- 竞赛管理
- 团队管理
- 学生教师管理
- 成果经费管理
- 作业演示页面

其中“作业演示页面”单独放置四个按钮或表单：

- 事务删除项目
- 触发器控制报名
- 存储过程更新项目状态
- 视图查询项目汇总

这样演示时可以直接按照报告顺序操作，不需要在多个页面之间来回切换。

## 7. 报告材料结构

`docs/report-outline.md` 按作业报告要求整理内容。

报告中需要准备以下内容：

- 项目信息
- 系统配置情况
- 数据库连接串及各部分分析
- 数据表创建顺序和表结构说明
- 数据库关系图截图
- 事务删除操作说明、涉及表、连接字段、关键代码和执行方法
- 触发器添加操作说明、输入条件、关键代码和执行方法
- 存储过程更新操作说明、涉及表、修改规则、关键代码和执行方法
- 视图查询操作说明、涉及表、连接字段、关键代码和执行方法

截图统一放在 `screenshots/` 下，便于最后整理进报告。

## 8. 开发顺序

项目按作业评分点推进。

1. 完成数据库建表脚本。
2. 插入演示数据。
3. 建立视图。
4. 编写触发器。
5. 编写存储过程。
6. 完成事务删除 SQL。
7. 写后端接口。
8. 写前端演示页面。
9. 截取数据库关系图和程序运行图。
10. 整理工程作业报告。

## 9. 当前项目范围

本项目只实现报告中需要展示的核心功能。

包含：

- 数据库表结构
- 演示数据
- 事务删除
- 触发器添加
- 存储过程更新
- 视图查询
- 核心管理页面
- 首页统计图
- 报告截图材料

